# Puerto de la UI Streamlit
UI_PORT=8501

# API orquestadora local
ORCHESTRATOR_URL=http://host.docker.internal:8765
ORCHESTRATOR_TOKEN=dev-orchestrator-token
ORCHESTRATOR_HOST=0.0.0.0
ORCHESTRATOR_PORT=8765

# URLs locales esperadas
KEYCLOAK_URL=http://localhost:8080
VAULT_ADDR=http://localhost:8200
VAULT_TOKEN=root

# Realm de demo
KEYCLOAK_REALM=logistics-dataspace

# Los valores de este archivo son para entorno de demostracion local.
